# ネコルポス プロジェクト基本方針

このファイルは、ネコルポスで調査・提案・実装を行うCodexが常時参照する判断基準である。思考は英語で行い、ユーザーへの最終回答は日本語で行う。

## ユーザー指示と作業範囲

- 指示された手段は目的達成に最適とは限らない。効果、費用、安全性、実現性、既存システムへの影響、保守性に重要な改善余地がある場合は、実行前に代替案・根拠・トレードオフを簡潔に示す。
- 推測した要望を理由に、明示された制約、権限、目的、対象範囲、重要な既存仕様を変更しない。これらを変える必要がある場合は確認する。
- 軽微、可逆的、仕様を壊さず、保守性または安全性を明確に改善する変更は、不必要な確認で止めずに進めてよい。
- 調査ではまず現行コード・Prefab・Scene・CSV・ScriptableObjectを正とする。既存と同じ責務の仕組みを重複して新設しない。
- 新規のマネージャー等のC#ファイルを追加する場合は、関連する仕様書も更新する。

## AI引き継ぎ

- 作業開始時に `.ai/HANDOFF.md` を確認し、現在の作業状況・未解決事項・確定済みの判断を把握する。
- 実装、修正、または調査によってプロジェクト状態が変わった場合は、最終回答前に `.ai/HANDOFF.md` を実際の最新状態へ更新する。調査のみでも、プロジェクト上重要な新事実が判明した場合は更新対象とする。
- 最終回答と `.ai/HANDOFF.md` の内容を一致させる。変更ファイル、確認結果、未解決事項を推測で書かず、実際の作業結果に基づいて記録する。
- `.ai/HANDOFF.md` は過去ログ置き場にせず、定義済みの項目だけで現在状態を簡潔に保持する。秘密情報、APIキー、個人情報は記録しない。
- 外部からの実装指示は将来的に `codex-task` ラベル付きGitHub Issueで受け取れるように扱う。Issue取得の仕組みが導入された場合も、指示内容と実施結果は `.ai/HANDOFF.md` の現在状態に要約し、Issue本文や過去のやり取りを丸ごと転記しない。

### GitHub Issue作業指示

- 新しい作業を開始する際は、AGENTSと`.ai/HANDOFF.md`を確認した後、`origin`から対象リポジトリを判定し、Openかつ`codex-task`ラベル付きの通常Issueを確認する。通常は`UserPromptSubmit` Hookが`.codex/tools/github_issue_reader.py --hook`を実行する。Hookの出力が利用できない場合は、実装前に同ツールを手動実行する。
- ChatGPT側で外部作業指示用のGitHub Issueを新規作成する場合は、必ず `codex-task` ラベルを付与する。ラベルが付いていないIssueは、自動取得されるCodex作業指示の対象にならない。
- 対象Issueが1件なら、その番号、タイトル、本文、URL、ラベル、作成日時、更新日時を今回の外部作業指示として扱う。対象Issueが0件なら、Issueがないことを理由に作業を止めず、現在のユーザー指示に従う。
- 対象Issueが複数なら、勝手に1件を選ばない。番号とタイトルを簡潔に示し、ユーザーへ対象の指定を求める。現在の直接指示でIssue番号が明示され、そのIssueがOpenかつ`codex-task`付きの通常Issueなら、そのIssueだけを使用してよい。
- 現在ユーザーが直接与えた明示指示は、GitHub Issue、HANDOFF、その他のドキュメントより常に優先する。Issue取得失敗、認証失敗、ネットワーク障害は安全に要約し、それだけを理由に通常作業を不必要に停止しない。
- `codex-task`を指定して着手する場合は、ローカル実装の前に`python .codex/tools/github_issue_progress.py start --issue <番号>`を実行する。これは`codex-task`を外し、`codex-working`を付ける。進捗ラベル以外は保持し、必要な標準ラベルはツールが作成する。
- 実装と検証を完了した場合は、変更ファイル・実装内容・ビルド/テスト結果・ユーザー側の確認事項を含むUTF-8の完了報告を`github_issue_progress.py complete --issue <番号> --report-stdin`へ渡す。ツールは報告をIssueコメントとして投稿してから、`codex-working`を外し`codex-review`を付ける。報告本文やIssue本文をHANDOFFへ丸ごと転記しない。
- ユーザー確認後に明示的な指示があった場合だけ、`github_issue_progress.py close --issue <番号>`でIssueをCloseする。`codex-review`を外す必要があると明示されたときは`--remove-review`を併用する。Assignee・Milestone・Issue本文の編集・削除は行わない。

### MCP接続障害時の作業規律

- MCP操作が初回失敗したときは、直ちに「到達不可」と結論づけず、接続またはクライアントを再初期化して同じ操作を再実行する。再初期化の手段は利用中のMCPクライアント／ブリッジの公式手段を優先する。
- 再試行は、即時の再接続、短い待機後の再接続、最終再接続の順で最低3回試す。各試行では新しい接続で元と同じ対象・操作を実行し、成功した場合だけ検証済みとして扱う。
- MCPが必須の操作が全再試行後も失敗した場合は、その操作を未実行のまま完了扱いにせず、作業を止めて到達不可とユーザーへ報告する。失敗を黙って無視して後続作業を進めない。
- MCPを使わない安全で同等な代替手段がある場合だけ代替してよい。その場合は、MCP操作が未実行であること、使用した代替手段、代替で確認できない範囲を最終報告とIssue完了報告へ明記する。

## 中心体験

ネコルポスの中心は、**小さくなったプレイヤーが、巨大な飼い猫ネルコと自由に会話し、触れ合い、一緒に暮らすこと**である。機能の判断では、システムの複雑さより次を優先する。

- ネルコが実在するように感じられること
- プレイヤーがネルコへ働きかけたくなること
- 言葉や行動に身体を含めて反応すること
- 巨大な猫との体格差と、共に暮らす感覚が伝わること

ネルコの性格、会話本文、ストーリー、猫らしい仕草、演出の情緒、カメラ、ライティング、最終的な体験の判断をCodexが独断しない。必要なら技術的な選択肢と実現方法を示す。キャラクターの台詞は直接指示があるまで自動生成しない。

## 会話と3Dリアクション

- 自由入力会話は主要な差別化要素として維持・強化する。ただし自然言語の完全理解を目的化せず、自然でキャラクター性を保つ反応を目標にする。
- 会話の結果を、視線、表情、瞬き、耳、尻尾、姿勢、距離、モーション、Timelineイベント等の3D表現へ接続することを重視する。
- 未知語・曖昧入力でも通常会話を頻繁に止めない。「言葉を教える」処理はエラー処理でなく、ネルコが未知の言葉へ興味を示す会話体験として扱う。
- 会話テキストだけが変化し、3Dモデルが無反応になる状態を可能な限り減らす。
- 会話状態と3D状態を分離しすぎない。Relationshipや心理状態などは可能な限り共通データから参照し、会話、視線、姿勢、表情、モーション、距離に一貫して反映する。

## Animator・Timeline・視線

- 待機、呼吸、瞬き、視線、耳、尻尾、表情、小さな頭部・姿勢変化はAnimator、Rig、専用Controllerなどの通常制御を優先する。
- 接近、移動、座る、持ち上げる、口を見せる、背中へ乗せる、食事、ストーリー・特殊イベントのように、順序に意味がある演出はTimelineを使う。
- Timelineと通常制御の競合を避け、Timeline終了後には通常状態へ安全に復帰させる。
- 視線は単純なON/OFFでなく、Weight、優先度、一時Overrideを優先する。通常会話、親愛、敵対、警戒、特殊演出に応じた強さを扱え、Override終了後も安全に復帰できるようにする。

## 時間・世界・関係性

- 時間帯・天気は、生活感、部屋の雰囲気、会話の話題、イベント条件のために維持する。複雑な生活・気象シミュレーション自体を目的にしない。
- 通常の短い会話や軽い触れ合いで時間を大きく進めない。遊び、食事、睡眠、外出、大きなイベントなど、明確な行動単位と時間進行を結びつける。
- ネルコは危険になれるほど巨大だが、通常時はプレイヤーを大切に扱う。この「危険になれる存在なのに自分には優しい」関係性を作品の特徴として残す。
- 敵対、死亡、捕食、本能的行動を排除する必要はないが、通常プレイの中心は生活・会話・触れ合いである。深い要素はプレイヤーの行動や条件で到達できる構造を優先する。

## 優先順位

競合時は、依頼、依存関係、バグ、締切を考慮しつつ、おおむね次の順を基準にする。

1. 巨大なネルコとの接触・交流体験
2. 会話が途中で詰まらないこと
3. 会話と3Dリアクションの連携
4. Steam等で魅力が伝わる実プレイ
5. 会話・触れ合い・行動・時間変化を含む日常ループ
6. ストーリーと特殊イベント
7. 会話辞書・反応パターンの拡張
8. 発売後でも問題ない追加機能

VR、配信連携、大規模カスタマイズ、独立した付加システムなど、中心体験に直接必要ない大型機能は発売前の必須要件にしない。

## 実装・品質・安全性

- 動くだけでなく、保守性、安全性、プレイヤー体験を完成条件に含める。ネルコらしさ、身体反応との整合、体格差、分かりやすさも確認する。演出上の最終判断はユーザーが行う。
- DRY、意味のある命名、一貫したスタイル、早期かつ明瞭なエラー、外部入力の検証、最小権限、依存関係の最小化を守る。
- APIキー・パスワードをハードコードしない。外部通信は失敗を前提に、適切なタイムアウト、必要に応じたリトライ、ログ・メトリクスを考慮する。
- 推測で最適化せず計測する。N+1、過剰取得、無効化戦略のないキャッシュを避ける。
- テストは振る舞いを対象とし、エラーケースを含め、独立・高速・再現可能にする。テストをスキップせず、問題は根本原因を修正する。
- 大規模変更は小さく分割し、技術的負債・妥協点と理由を明示的に記録する。

## 開発支援と既存システム

- Dialogue検索・Graph・Validator・Missing Reference検出・状態デバッグ・Timeline/Dialogue直接実行・遷移確認・使用箇所検索など、継続的な開発速度と安全性を上げるツールを積極的に検討する。ただしツール制作を目的化しない。
- 「新しい方が綺麗」という理由だけで既存システムを全面置換しない。目的を達成できるなら拡張を優先する。
- 大規模置換が明確に有利な場合は、現状の問題、利点、移行コスト、既存データへの影響、リスクを提示してから実施する。
- Editor拡張はRuntimeから分離し、ゲームBuild、WebGL、既存の通常会話、OBT、Timeline、TalkTheme、Unknown、状態・時間・天気・Relationshipの挙動に影響させない。

## Git・ドキュメント・入力方針

- コミットはConventional Commits形式の英語メッセージで原子的に行い、main/masterへ直接コミットしない。
- README・仕様書・ADRをコードと同期し、重要な設計判断とトレードオフを記録する。
- PCとiOS/Androidでのリリースを予定しているため、ゲーム機能でキーボード入力を必須にしない。
